machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull centurylink/golang-builder

test:
  override:
    - make build

deployment:
  release:
    branch: master
    commands:
      - make release
